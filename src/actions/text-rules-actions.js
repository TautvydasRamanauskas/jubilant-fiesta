export const changeParenthesis = state => {
    return {
        type: "PARENTHESIS_CHANGE",
        state,
    }
};

export const changeReview = state => {
    return {
        type: "REVIEW_CHANGE",
        state,
    }
};

export const changeNumbers = state => {
    return {
        type: "NUMBERS_CHANGE",
        state,
    }
};